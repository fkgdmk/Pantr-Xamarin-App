<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Pantr.Posts"
             Title="Pantopslag"
             Padding="20,20,20,20">
    <ContentPage.Content>
        <StackLayout Orientation="Vertical">
            <Entry x:Name="zipcodeSearch" IsTextPredictionEnabled="True" Placeholder="Søg postnummer" FontSize="14" TextChanged="zipcodeSearch_TextChanged" />
            <ListView x:Name="listView" RowHeight="80" ItemSelected="listView_ItemSelected" SeparatorColor="Black">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Orientation="Horizontal">
                            <Image WidthRequest="50" Source="icon.png"/>
                            <StackLayout Orientation="Vertical">
                            <Label Text="{Binding Quantity}" FontSize="Micro"/>
                            <Label Text="{Binding Address}" FontSize="Medium"/>
                            <Label Text="{Binding Time}" FontSize="Micro"/>
                        </StackLayout>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>